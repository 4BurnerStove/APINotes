// const express = require('express')

// const app = express()

// Obrigatorio para encontrar a rota
// app.get('/message/:id/:user', (request, response) => {
//   const { id, user } = request.params
//   const responseTest = `O Id da mensagem é ${id}, para o usuário ${user}`
//   response.send(responseTest)
// })

// Opicional, flexivel para acessar 
// app.get('/users', (req, res) => {
//   const { page, limit } = req.query
//   const queryTest = `Página: ${page}. Mostrar: ${limit}`

//   res.send(queryTest)
// } )

// const PORT = 3333
// app.listen(PORT, () => console.log(`Server is running on port ${PORT}`))
